name: Upload Release Candidate to S3 and verify it
on:
    release:
        types:
            - published
jobs:
    pack-from-tag:
        name: Pack a release candidate archive from the tag ${{ github.event.release.tag_name }}
        runs-on: 'ubuntu-latest'

        steps:
            - name: Log
              run: |
                  echo ${{ github }}
                  echo ${{ github.event }}
                  echo ${{ github.event.release }}
                  echo ${{ github.event.release.tag_name }}
                  echo ${{ github.event.release.prereleased }}
            - name: Checkout to tag ${{ github.event.release.tag_name }}
              uses: actions/checkout@v2
              with:
                  ref: ${{ github.event.release.tag_name }}
            - name: Setup Node.js
              uses: actions/setup-node@v2
              with:
                  node-version: 12
            - name: Install dependencies
              run: npm install
            - name: Compile Typescript to JavaScript
              run: npm run compile
            - name: Pack an archive
              run: npm pack

    upload-to-s3:
        name: Upload the pack to S3
        runs-on: ubuntu-latest

        needs: pack-from-tag

        steps:
            - name: Upload the pack to S3
              run: aws s3 mv ./hazelcast-client-*.tgz s3://nodejs-client-release-candidate-packs/${{ github.event.release.tag_name }}.tgz
              env:
                  AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
                  AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
            - name: Make uploaded object publicly available
              run: aws s3api put-object-acl --bucket nodejs-client-release-candidate-packs --key ${{ github.event.release.tag_name }}.tgz --acl public-read
              env:
                  AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
                  AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
    verify-uploaded-pack:
        name: Verify the uploaded pack
        runs-on: ubuntu-latest

        needs: upload-to-s3

        steps:
            - name: Download the pack
              run: wget https://nodejs-client-release-candidate-packs.s3.amazonaws.com/${{ github.event.release.tag_name }}.tgz
            - name: Unarchive the pack
              run: tar xvzf ${{ github.event.release.tag_name }}.tgz
            - name: Setup Java
              uses: actions/setup-java@v1
              with:
                  java-version: 8
            - name: Replace lib folder
              run: |
                  rm -rf lib
                  mv package/lib .
            - name: Lint
              run: |
                  npm run lint
            - name: Validate User Code
              run: |
                  npm run validate-user-code
            - name: Run all tests
              env:
                  HAZELCAST_ENTERPRISE_KEY: ${{ secrets.HAZELCAST_ENTERPRISE_KEY }}
              run: |
                  npm run coverage

